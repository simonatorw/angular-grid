import { GreyDirective } from './grey.directive';

describe('GreyDirective', () => {
  it('should create an instance', () => {
    const directive = new GreyDirective();
    expect(directive).toBeTruthy();
  });
});
